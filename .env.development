# Liberation Analytics - Local Development Environment
# This file is for local development only

# Environment Configuration
ENVIRONMENT=development
ANALYTICS_HOST=0.0.0.0
ANALYTICS_PORT=8080

# Database Configuration (Local Development)
POSTGRES_PASSWORD=dev_password_change_me
POSTGRES_DB=liberation_analytics_dev
POSTGRES_USER=postgres
DATABASE_URL=postgres://postgres:dev_password_change_me@postgres-dev:5432/liberation_analytics_dev

# DuckDB Configuration (Local Development)
DUCKDB_PATH=./data/analytics.db

# CORS Configuration (Local Development)
# Allow all common local development origins
ALLOWED_ORIGINS=http://localhost:3333,http://127.0.0.1:3333,http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001

# Security Configuration (Development - Less Secure)
API_TOKEN_SECRET=dev_secret_not_for_production_use_only
RATE_LIMIT_ENABLED=false
RATE_LIMIT_REQUESTS_PER_MINUTE=1000

# Optional: GeoIP (Disabled for development)
GEOIP_ENABLED=false
GEOIP_DATABASE_PATH=./data/GeoLite2-Country.mmdb

# Logging Configuration
LOG_LEVEL=debug
GIN_MODE=debug

# Volume Paths (Local Development)
ANALYTICS_DATA_PATH=./data/analytics
POSTGRES_DATA_PATH=./data/postgres

# Network Configuration
ANALYTICS_NETWORK=analytics-dev-network